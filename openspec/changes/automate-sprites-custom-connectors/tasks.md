## 0. Discovery (verified / to verify)

- [x] 0.1 Historical dashboard create flow and deny-all default (spike 2026-07-06; superseded by REST creation).
- [x] 0.2 REST `POST /v1/oauth/connections/custom_api` creates Custom API connectors with a final `access_policy` and returns the authoritative gateway connection id (Fly support + live verification 2026-07-26).
- [x] 0.3 Gateway does NOT forward a verifiable Sprite identity upstream → per-session internal connector.
- [x] 0.4 Transparent egress proxy works (MITM, auth stripping, gateway rewrite/injection, REDIRECT captures curl/Node/Python).
- [x] 0.5 Sprite has passwordless sudo; `sudo apt-get install -y nftables iptables` works; NAT REDIRECT diverts live connections; `cap_net_admin`+`cap_sys_admin`.
- [x] 0.6 Network policy is enforced L3/L4 outside the VM (IP-direct to non-allowlisted hosts refused) — restricted modes have a hard boundary that in-VM root cannot lift; `open` intentionally does not.
- [x] 0.7 In-VM root cannot change its own Sprite's Fly labels; the Sprites API can set labels at creation or update them afterward → label scoping is safe.
- [x] 0.8 Access policy has NO Sprite-id field (`sprite_labels`/`name_prefix` only); policy update is whole-object replacement → scope by label, set once at mint, never edit per session (no concurrency race).
- [x] 0.9 Current webhook auth: VM posts to `/internal/session/:sessionId/chunks` and `/events` with `DO_WEBHOOK_TOKEN`; the Worker resolves the DO by route and the DO validates its SQLite `webhook_token`.
- [x] 0.10a Claude: user-owned refreshable OAuth remains in encrypted D1; live test proved Claude Code 2.1.207 interactive and non-interactive inference works with `ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN` while the Worker refreshes/injects OAuth downstream. A fake OAuth file does not bypass interactive login, and the Worker must add `oauth-2025-04-20` in gateway mode (`test:live:claude-oauth-control-plane-proxy`, 2026-07-23; see `provider-proxying.md`).
- [x] 0.10b Codex: Codex 0.144.3 completed `gpt-5.4` inference from a fresh Sprite through `webhooks.bze.llc` using a custom Responses provider and short-lived Cloude bearer. A native reqwest proxy validated the bearer, injected D1 OAuth + `ChatGPT-Account-ID`, stripped tunnel/proxy headers, and streamed the response. A manual follow-up launched the normal interactive TUI without `auth.json`. The same fresh request through local workerd received a ChatGPT-edge HTML `403`, while Node's ordinary native `fetch` succeeded. A same-machine transport probe isolated unavoidable `CF-Worker` metadata and a different TLS fingerprint as concrete differences, but the exact WAF rule is not observable (`test:live:codex-oauth-control-plane-proxy`, 2026-07-23).
- [x] 0.11 REST connection endpoints take the **gateway connection id**; delete is `DELETE /v1/oauth/connections/{id}` (proven by the passing disposable live test, 2026-07-24).
- [x] 0.12 End-to-end disposable REST mint: create returned `201` with label + `/headers` policy; a matching labelled Sprite received the injected credential; `/get` returned `403`; connector and Sprite cleanup both succeeded (2026-07-26).
- [x] 0.13 Gateway `Accept` allowlist (live probe `test:live:gateway-accept`, 2026-07-28): the gateway forwards only when Accept contains the literal `application/json` or the wildcard token, or is absent; everything else returns `406` before reaching the upstream (`text/event-stream`, `text/plain`, `text/*`, `application/octet-stream`, `application/vnd.api+json`, and git's `application/x-git-*`). `text/*` does not match `text/event-stream`, so it is a literal token allowlist, not content negotiation. Response content-types, query strings, deep wildcard paths, and custom user agents pass through. Webhooks are unaffected (vm-agent sends no Accept → wildcard). Git data therefore uses the connector-authenticated ephemeral-token mint plus the direct Worker proxy; 0.14 records the provider-streaming follow-up.
- [x] 0.14 Managed OpenRouter connector streaming probe (2026-08-27): non-streaming JSON succeeded; `stream:true` with `Accept: text/event-stream` returned `406`; and `stream:true` disguised with `Accept: application/json` returned 19 SSE `data:` records only after the full response was buffered (`Content-Length: 7832`, time-to-first-byte 1.6137s, total 1.6143s). The OpenRouter connector supports inference, including provider-produced SSE payloads, but the Sprites gateway is not a streaming data plane. S4 uses the connector only for a small JSON capability mint and sends inference bytes directly to the Cloude provider proxy.

## 1. Connector provisioning (`mintConnector`) — cross-cutting

- [x] 1.1 `mintConnector({...}) -> {gatewayConnectionId}`: REST create with the final deny-all label/path policy → REST verify by returned id → delete-on-failure. The per-attempt unique name remains the attribution key for uncertain-response reconciliation.
- [x] 1.2 Run in the API-server shared connector integration, with `@repo/sprites-client` owning the REST wire contract; remove the separate Worker, bearer, and service binding.
- [x] 1.3 Reuse the API server's existing Sprites API token; redact it and submitted connector credentials from responses and logs.
- [x] 1.4 REST create-with-final-policy + verify + delete; reconcile a retryable uncertain create response only by its unique per-attempt name and delete a match.
- [x] 1.4a `GET /v1/oauth/connections` returned only the `connections` collection with no cursor, continuation token, or pagination metadata; uncertain-response reconciliation uses that complete envelope (live verification 2026-07-26).
- [x] 1.5 Verify label immutability from a root shell inside a Sprite (2026-07-26, disposable `hostile-root-test-7f3a9c`): no injected control-plane credential; root CLI label mutation failed with no organization configured; direct management `PUT`/`PATCH` and child-Sprite creation returned 401; the internal API exposed no label mutation path; external read-back preserved `security-test:baseline-7f3a9c`; Sprite deleted.
- [x] 1.5a Land Sprite labeling and connector label enforcement in the same integration change, not sequentially. Sprite creation now sends `session:<sessionId>` (plus `env:<environmentId>` when present) via `@fly/sprites` 0.1.0 `labels`, and the mint verifies the labels are present before creating the connector.
- [x] 1.6 Endpoint pinning: `mintConnector` accepts `allowedEndpoints`, includes it in REST creation, verifies it on read-back, and requires it for class-A `session:` connectors only. A matching labelled disposable Sprite succeeded on `/headers` and received `403` on off-pin `/get` (2026-07-26).
- [x] 1.7 Remove the separate connector-provisioner HTTP service. Connector lifecycle is now an in-process API-server operation, so there is no independently exposed `/mint` bearer to rate-limit.

## 2. Data model (D1) — cross-cutting

- [x] 2.1 `session_connectors` (internal): session_id, gateway conn id, policy summary, status, timestamps. Keep the webhook token in DO SQLite, not D1. Migration `0029_session_connectors.sql` + `SessionConnectorsRepository`.
- [ ] 2.2 `environment_connectors` (class B, metadata only — Sprites custodies value): id, environment_id, name, upstream origin, header name/prefix, gateway conn id, label `env:<environmentId>`, lifecycle status, timestamps; unique per environment + hostname. Statuses cover provisioning, active, replacement-pending, pending-revocation, and error so the deletion id is never lost.
- [ ] 2.3 Query environment connector metadata before Sprite creation to build labels and the routing table; never store plaintext or session membership.
- [ ] 2.4 Zod schemas/types for session connectors, environment connectors, access-policy scopes, and provisioning states.

## 3. Network egress policy — cross-cutting

- [x] 3.1 Preserve `buildFinalNetworkPolicy` semantics for `open`, `default`, `custom`, and `locked`; add connector-gateway reachability to restricted modes. Apply only the temporary provisioning exceptions needed for toolchain install, then the user's selected final policy before the agent runs. `connectorGatewayHostname` is added to bootstrap and to every restricted mode; `open` is unchanged.
- [ ] 3.2 In restricted modes, do not add class-A/B credential hosts merely because a connector exists; preserve any host already allowed by the selected default/custom policy. Provider inference permits only the session connector's JSON capability-mint path and the direct Cloude provider-proxy hostname. Test that direct access never receives a connector credential, including under `open`.

## S1. Connector spine + webhook

- [x] S1.1 Create the Sprite with `session:<sessionId>` and `env:<environmentId>` labels, then mint the internal connector (base = Worker, token = existing DO webhook token, policy = session label); store connector metadata in D1 and fail closed. `mintConnector` enforces per-attempt name uniqueness (appends a random suffix), making name attribution definitive across concurrent duplicate mints; store the returned `name` alongside the ids (2026-07-25). Runs as the blocking, retryable `session_connector` setup task between `cloud_container` and `repository`.
- [x] S1.2 Preserve `/internal/session/:sessionId/chunks` and `/events`; the route resolves the DO and the DO validates the gateway-injected token from SQLite. Add a connector health `test_url` without introducing a generic `/webhook` or D1 secret mapping. Routes and DO validation are unchanged; `test_url` is the existing unauthenticated `/health`.
- [x] S1.3 Give the VM the connector gateway base instead of `DO_WEBHOOK_TOKEN`; cut over behind a flag and retire Sprite-held webhook-token delivery once proven. Unconditional for new sessions (flags removed after local testing was chosen over a staged rollout); the VM gets `DO_WEBHOOK_AUTH=gateway` and posts without an Authorization header. Pre-connector sessions keep Sprite-held delivery, logged per spawn; removing that legacy path remains a follow-up.
- [x] S1.4 Teardown revokes ephemeral Git tokens and deletes the DO webhook token before slow external cleanup, then deletes the internal connector. It does not edit class-B policies. `deleteForTeardown` marks `pending_revocation`, deletes + verifies, then removes the row.

## S2. Git cutover

- [x] S2.1 Git cutover for the gateway `Accept` limitation: the session connector exposes only an exact JSON token-mint endpoint. The DO validates the connector-injected webhook token and issues a five-minute opaque token from its SQLite secrets store. A repo-local credential helper obtains it, then fetch/push go directly to the Worker Git proxy with HTTP Basic. Ephemeral-token mode rejects the legacy bearer and webhook token and preserves per-request repository access. Pre-connector sessions remain in explicit `legacy_secret` mode.
- [x] S2.2 Preserve Worker-custodied installation token, `cloude/*` branch validation + lock, repo allowlist, `locked` policy. Unchanged; only the accepted bearer and the remote URL differ.
- [x] S2.3 Initial clone remains on direct GitHub with its short-lived contents-read-only token. New sessions no longer create `gitProxySecret`; both later fetch and push use the direct Worker proxy with rotating ephemeral tokens.

## S4. Provider inference through the control plane

- [ ] S4.0 Fix the existing server-side OAuth refresh race in the AI-auth area before production: when two sessions refresh the same user/provider record concurrently, serialize them or use optimistic versioning so a rotated refresh token cannot be overwritten. Test concurrent sessions and refresh-token rotation. This is not Sprite token refresh.
- [x] S4.1a Claude compatibility spike: local Worker reads/refreshes OAuth from D1, overwrites the Sprite's non-provider bearer, adds `oauth-2025-04-20`, streams Anthropic inference, and rejects an invalid gateway token.
- [x] S4.1b Codex base compatibility spike: custom Responses provider + short-lived non-provider bearer in a fresh Sprite, authenticated native reqwest egress, D1 OAuth + account-ID injection, tunnel-header stripping, streamed `gpt-5.4` inference, invalid-bearer rejection, and normal interactive TUI startup without `auth.json`.
- [ ] S4.1c Deploy one shared stateless Node provider-inference proxy on ordinary VM/container egress. It serves both Claude and Codex, accepts only short-lived Cloude inference admission tickets, obtains current provider access material from an authenticated Worker control RPC, and never receives refresh tokens. Prove both Anthropic and ChatGPT egress before rollout.
- [ ] S4.2 Add the exact JSON class-A route `/internal/session/:sessionId/inference-token`. The session connector authorizes the Sprite and injects the existing session credential; the DO validates it and mints a five-minute opaque admission ticket scoped to that session and requested provider. Store/revoke its verification state with session secrets. Despite the route name, this is a Cloude proxy admission ticket, not a provider token or refreshable harness credential; no provider connector, owner label, or connector table is added.
- [ ] S4.3 Host a Sprite-local inference proxy inside the existing Bun vm-agent. It listens only on `127.0.0.1`, starts before either CLI, shares the vm-agent lifecycle, and fails provider startup closed if unavailable. Claude uses `ANTHROPIC_BASE_URL=http://127.0.0.1:<port>/claude` plus literal `ANTHROPIC_AUTH_TOKEN=cloude-placeholder`; Codex uses the localhost custom Responses provider plus a literal `CLOUDE_CODEX_AUTH_TOKEN` placeholder. For each actual provider HTTP request, the local proxy mints one admission ticket through the connector, removes the untrusted placeholder, calls Node directly, and streams without the Sprites gateway. It does not cache, refresh, or synchronize tickets with the CLI harness.
- [ ] S4.4 Implement the Node/Worker trust boundary: Node validates the admission ticket through one authenticated Worker/DO control RPC; the Worker resolves the session user, refreshes the existing encrypted provider record, and returns only the current access material required for that request. For Claude, Node removes `x-api-key` and competing authorization, sets the current OAuth bearer, and ensures `anthropic-beta` includes `oauth-2025-04-20`. For Codex, Node sets the current OAuth bearer and `ChatGPT-Account-ID`. It restricts provider paths, strips tunnel/proxy/client credentials, and streams directly to the provider. The DO, connector gateway, and Worker never carry inference bodies.
- [ ] S4.5 Prove the admission boundary: another Sprite and off-Sprite callers cannot mint; an extracted ticket contains no provider credential, is provider/session scoped, expires within five minutes, and is rejected after teardown/revocation. Prove the local proxy neither caches nor background-refreshes tickets, and that cancellation or ticket expiry does not terminate a stream already admitted.
- [ ] S4.6 Provider acceptance: Claude interactive + `-p`; Codex interactive + app-server; placeholder-only Sprite inspection; `/models` if required; streamed text/tools/long responses; interruption/resume/retry; errors; compaction; path allowlists; OAuth refresh/revocation; concurrent capacity/backpressure; and documented custom-base feature limitations.

## S3. Transparent proxy data plane (after S4; class B only)

- [ ] S3.1 Install nft/iptables at provisioning (`sudo apt-get`); OUTPUT REDIRECT only dummy-destination tcp/443 to the proxy; class-C and gateway real destinations bypass it; fail closed.
- [ ] S3.2 Per-Sprite CA + per-host SNI leaf certs; install into system + per-runtime trust stores; enumerate/handle trust-store-bypassing runtimes.
- [ ] S3.3 Local resolver returns a reserved dummy IPv4 address and suppresses AAAA for class-B hosts; class-C and gateway hosts resolve normally.
- [ ] S3.4 Generalize `sprite-egress-proxy.mjs` to one connector per protected hostname with fail-closed default; strip the configured client credential header and stream bodies. Class-C never enters the proxy.
- [ ] S3.5 Webhook and the Git/inference capability mints use direct gateway URLs; Git smart-HTTP uses the direct Worker URL and provider streams use the direct Node proxy. None enters the class-B proxy. Install the data plane only for environments with class-B credentials; lifecycle (start at provision, tear down all of it at end).
- [ ] S3.6 V1 protocol contract: advertise HTTP/1.1, support streaming and required HTTP/1.1 WebSocket upgrades; reject/document HTTP/2-only, gRPC, HTTP/3, alternate ports, non-header auth, and multiple credentials per hostname.

## S5. Environment header credentials

- [ ] S5.0 Obtain and live-test the REST field for custom header names before accepting arbitrary header names. Until it passes, the implemented connector contract remains `Authorization` only.
- [ ] S5.0a Verify Sprites rejects connector targets that resolve/rebind to loopback, link-local, private, metadata, or reserved ranges and never forwards an injected credential across an origin-changing redirect. If not, implement resolved-address and redirect enforcement before exposing user-defined origins; connection-test success alone is not the safety check.
- [ ] S5.1 Definition UI/API: an environment declares one header credential (name, value, public HTTPS upstream origin, header name/prefix) per hostname, with optional endpoint restrictions.
- [ ] S5.2 Mint a class-B connector for the environment + hostname (value → Sprites custody; store metadata only in D1) with immutable `sprite_labels: [env:<environmentId>]`.
- [ ] S5.3 At provisioning, put `env:<environmentId>` on the Sprite and add the environment's connector hosts to the routing table. Never edit or de-scope the connector policy per session.
- [ ] S5.4 Environment/credential deletion revokes the affected class-B connector independently: stop resolving it for new sessions, confirm connector deletion or verified deny-all before removing metadata, and retain a pending-revocation record for reconciliation on partial failure. Repository-access blocking does not remove `env:` labels, revoke the session token, or delete the Sprite.
- [ ] S5.5 Edit/rotation uses replacement, not in-place secret mutation: mint/scope/verify a new connector, atomically swap the active D1 id, refresh active sessions' non-secret routes where possible, and revoke the old id. A stale active-session route fails closed until refreshed/restarted; never retain the old credential solely for continuity.

## 6. Session provisioning (synchronous, fail-closed) — cross-cutting

- [ ] 6.1 Ordered flow: resolve environment connectors → create labelled Sprite + temporary bootstrap policy → mint/verify session connector → install data plane → apply the user's selected final network mode + gateway reachability → hand Sprite non-secret config → start agent. Fail closed on protected-route setup without changing `open`/`default`/`custom`/`locked` semantics.
- [ ] 6.2 Teardown: delete internal connector + DO token, data plane, and Sprite. Never edit shared class-B connector policies.

## 7. Tests And Validation

- [x] 7.1 API-server connector tests: REST request shape, create/verify success, uncertain-response reconciliation, validation, and fail-closed cleanup on policy mismatch.
- [~] 7.2 D1 tests: session connector metadata lifecycle, create cleanup, and status transitions are covered in `session-connector.service.test.ts`. Environment/class-B rows, uniqueness, replacement ordering, and revocation reconciliation remain for S5.
- [ ] 7.3 Data-plane tests: routing table, configured-header stripping, CA trust, dummy resolver, targeted redirect, class-C/gateway bypass, fail-closed default.
- [ ] 7.4 Identity tests: off-Sprite webhook/environment credential replay and Git/inference capability minting are rejected; a still-valid extracted ephemeral Git or inference capability remains replayable only within its narrow scope until expiry or DO revocation; another Sprite is denied.
- [ ] 7.5 Provisioning tests: create/update label wrapper, class-A session-label authorization, class-B environment-label authorization, synchronous fail-closed, teardown without class-B policy mutation.
- [ ] 7.5a Authorization-lifecycle tests: repository blocking still rejects new turns and every Git/PR request while preserving the Sprite/session/environment authorities; environment or credential deletion makes its class-B connector unusable without deleting unrelated sessions.
- [ ] 7.5b Network-mode tests: connectors work in `open`, `default`, `custom`, and `locked`; each mode preserves its prior direct-egress semantics; direct upstream access never gains the connector credential.
- [x] 7.6 `pnpm build`, `pnpm lint`, `pnpm typecheck`, relevant package tests.
